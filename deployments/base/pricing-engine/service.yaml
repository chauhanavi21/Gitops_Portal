apiVersion: v1
kind: Service
metadata:
  name: pricing-engine
  labels:
    app: pricing-engine
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: pricing-engine
